resources:
  - name: dv-image-tm
    type: image
    integration: deepika-docker
    pointer:
      sourceName: "library/nginx"
    seed:
      versionName: latest

  - name: time-resource
    type: time
    seed:
      interval: "*/2 * * * *"
