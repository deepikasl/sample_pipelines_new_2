resources:
  - name: dv-image
    type: image
    integration: deepika-docker
    pointer:
      sourceName: "library/nginx"
    seed:
      versionName: latest

  - name: time-resource
    type: type
    seed:
      interval: "*/2 * * * *"
